import numpy as np

arr = np.array([[1, 2, 3], [4, 5, 6]])  # To create an array in numpy
print(arr)  # to print the array
print(type(arr))  # to print the type of arr created
print()

#  To print the number of element in the array:
print("Number of elements in array :", np.size(arr))

#  To print dimensions of array :
print("Dimensions of array are :", np.shape(arr))


